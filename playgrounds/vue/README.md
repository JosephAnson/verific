# Verific Vue playground

This app exercises implicit validation scopes, descendant registrations and the Vue I18n adapter. Validate a form, then switch locale to see its derived errors update without another validation run.

```bash
# From the repository root
pnpm dev:vue

# Type-check and build the playground
pnpm --dir playgrounds/vue build
```
